void swap(int *a, int *b){
    int t;
    t = *a;
    *a = *b;
    *b = t;
}

int main(){
    int x;
    int y;
    x = 3;
    y = 7;
    swap(&x, &y);
    putint(x);
    putch(10);
    putint(y);
    putch(10);
    return 0;
}
